version: '3.8'

services:
  app:
    build: .
    container_name: linuxdo-review
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
